# =============================================================================
# LTI — Environment variables
# NEVER commit this file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL / Prisma
# -----------------------------------------------------------------------------

# Database user (must match POSTGRES_USER in Docker Compose)
DB_USER=LTIdbUser

# Database password
DB_PASSWORD=D1ymf8wyQEGthFR1E9xhCq

# Database name
DB_NAME=LTIdb

# DATABASE_URL for LOCAL development (outside Docker).
# When running inside Docker Compose the DATABASE_URL is overridden by compose
# to use the "db" service hostname instead of localhost.
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@localhost:5432/${DB_NAME}"

# -----------------------------------------------------------------------------
# Backend
# -----------------------------------------------------------------------------

# Comma-separated origins allowed by the CORS middleware.
# Default when not set: http://localhost:3000
# CORS_ORIGIN=http://localhost:3000

# -----------------------------------------------------------------------------
# Frontend build (React / Create React App)
# -----------------------------------------------------------------------------

# Base URL for all API calls made from the browser.
# Leave empty ("") to use the Nginx reverse proxy (recommended for Docker).
# Set to an absolute URL (e.g. http://localhost:8080) for local dev without Docker.
# REACT_APP_API_URL=
